# DESIGN.md

## 气质与意象
- 关键词：终端会话、工程文档、答案优先
- 场景：开发者在终端里敲下 `/status`，屏幕直接给出答案——不是仪表盘，而是一次查询的返回结果。像 `man page` 一样可信，像 HN 一样克制。
- 参考锚点：codex-reset.com —— 首屏第一句话直接回答 "Did Codex reset today? No."，用 `❯` 提示符、ASCII 进度条、真实推文流建立信任。

## 视觉策略
- **答案优先**：首屏第一句话必须是"今天会重置吗？不会。"——纯文字，无装饰
- **概率语气匹配**：低/中/高概率分别使用“暂不太可能 / 有一定可能，继续观察 / 很可能”，避免中等概率被表述成确定结论
- **文档流布局**：章节用分隔线和标题组织，不用卡片堆叠；像一篇写得很好的 README
- **真实数据流**：信号雷达展示为时间线信息流（推文 + 分类标签），不是 2x2 卡片网格
- **终端原生元素**：`❯` 提示符、`████████░░` ASCII 进度条、`/status` 命令引用
- **零装饰动画**：删除 glow-pulse、live-breathe、fade-in-up、signal-glow；唯一允许动效是 LIVE 小圆点的呼吸

## 配色方案
- 背景：#0B0C0F (225 15% 5%) — 终端底色
- 表面：#16171C (230 12% 10%) — 极少量使用，仅用于输入框、表格行
- 主色：OpenAI Green #10A37F (162 82% 35%) — 仅用于关键数据、链接、活跃状态
- 文字：#F0F2F5 (216 20% 95%)
- 次要文字：#7C8494 (220 10% 53%)
- 警告：#F59E0B (38 92% 50%) — 仅用于真实警告
- 危险：#EF4444 (17 94% 57%) — 仅用于过期/错误
- 边框：#26272E (232 10% 16%) — 分隔线优先于卡片边框

## 字体排版
- 正文：Inter — 保持
- 数据/终端元素：IBM Plex Mono — 扩大使用范围，终端风格的区块整体用 mono
- 中文回退：PingFang SC / Microsoft YaHei
- Hero 答案文字：`text-4xl~5xl font-semibold`，直接用文字说话
- 章节标题：`text-lg font-semibold`，前面加 `##` 或 `❯` 终端符号

## 动效与交互
- 禁止入场动画（fade-in-up 系列全部移除）
- 禁止发光/脉冲效果（glow-pulse、signal-glow 移除）
- 仅保留：LIVE 圆点 2s 呼吸、hover 颜色过渡 150ms
- 进度条用 ASCII 字符渲染，不用 div 宽度动画

## 布局原则
- 单列文档流为主，最大宽度 `max-w-3xl` 居左（不居中）
- 章节间距用 `my-10` + `border-t border-border/30`，不用卡片
- 数据密度高：行内统计（`30% in 24h · 50% in 48h`）优于独立大卡片
- 信息流用时间线样式，左侧时间戳，右侧内容

## 组件规范
### 信号雷达（SignalPanel）
- 状态标签用**反色块**（reverse video）：ACTIVE=主色底深字、WARM=琥珀底深字、IDLE=暗文字无底——不活跃的元素不许抢视觉
- 信号强度用 ASCII 条（`█`/`░`，与 Hero 概率条同一词汇）+ 百分比，颜色随状态
- 区块头部有聚合读数行：`composite ████░░ 47% · 2/4 active`，mono 小字
- 排序：状态严重度优先（ACTIVE>WARM>IDLE），同档按时间倒序
- ACTIVE 行允许 `bg-primary/5` 极浅底色（-mx-2 px-2 呼吸），其余行无底色

### 订阅 CTA（ResetAlertsPanel）
- 主按钮用**实心反色块**：`bg-primary text-background` + mono uppercase 追踪加宽——全站唯一的主行动点，允许最响
- 整体是一个**终端监视井**：单层描边容器（`border-border/40 bg-muted/20`，无圆角），头部是命令回显行（`❯ watch confirmed-resets --channel email`）。提醒只在重置确认后投递，不显示不存在的预测阈值；`focus-within` 时整井边框染绿——像一条正在运行的 watch 命令
- 标题右侧挂**布防状态标签**：已订阅或推送开启=反色块 `ARMED`（主色底深字），否则暗文字 `STANDBY`——状态词汇与信号雷达的反色标签同一语法
- 输入行是井内命令行：`❯` 提示符 + 无边框透明输入（井本身已是容器，不再嵌套井）
- 次级动作（推送开关）用**描边块**而非实心：`border-border/40` hover 提边色——层级次于主 CTA
- 成功态：`✓` 字形 + 主色 mono 单行，不弹 toast
- 全站按钮三档词汇固定：实心反色（主）/ 描边块（次）/ 纯文字链接（弱）

## 设计禁忌
- 禁止蓝紫渐变、发光边框、脉冲动画、入场级联动画
- 禁止大数字 + glow 的"AI 仪表盘"风格
- 禁止卡片套卡片的嵌套布局
- 禁止无意义的图标装饰（每个图标必须承载信息）
- 禁止亮色模式
- 禁止居中对称的 marketing 式 hero 布局
